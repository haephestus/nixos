{
  "runclientdebug": {
    "prefix": "runclientdebug",
    "body": [
      "tasks.named('runClient') {",
      "    jvmArgs '-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5005'",
      "}"
    ],
    "description": "Enable JDWP debug agent on runClient (:5005) for nvim-dap remote attach"
  }
}

# modules/containers

| File | Type | Status | Purpose |
|---|---|---|---|
| `docker.nix` | NixOS | staged (stub) | No-op placeholder (`{ }`). Not imported. Container needs are currently covered by podman via `modules/virtualization/distrobox.nix`. |
